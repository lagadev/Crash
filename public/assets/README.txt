Drop your own animated GIFs here (optional):
  flying.gif   -> shown while the rocket is climbing (round "running")
  crashed.gif  -> shown for a few seconds right after the crash

If these files are missing, the app automatically falls back to a simple
built-in emoji animation (🚀 / 💥), so the game works out of the box even
without custom art.
